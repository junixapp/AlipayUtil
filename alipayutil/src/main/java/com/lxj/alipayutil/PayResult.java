package com.lxj.alipayutil;import android.text.TextUtils;public class PayResult {	public static final String SUCCESS = "9000";//支付成功	public static final String WAIT_CONFIRM = "8000";//支付结果确认中	public static final String CANCEL = "6001";//用户取消支付	public static final String NET_ERROR = "6002";//网络连接出错	private String resultStatus;//支付状态	private String result;//需要验证的信息，包含了所有的签名信息	private String memo;//支付描述信息	public PayResult(String rawResult) {		if (TextUtils.isEmpty(rawResult))			return;		String[] resultParams = rawResult.split(";");		for (String resultParam : resultParams) {			if (resultParam.startsWith("resultStatus")) {				resultStatus = gatValue(resultParam, "resultStatus");			}			if (resultParam.startsWith("result")) {				result = gatValue(resultParam, "result");			}			if (resultParam.startsWith("memo")) {				memo = gatValue(resultParam, "memo");			}		}	}	@Override	public String toString() {		return "resultStatus={" + resultStatus + "};memo={" + memo				+ "};result={" + result + "}";	}	private String gatValue(String content, String key) {		String prefix = key + "={";		return content.substring(content.indexOf(prefix) + prefix.length(),				content.lastIndexOf("}"));	}	/**	 * @return the resultStatus	 */	public String getResultStatus() {		return resultStatus;	}	/**	 * @return the memo	 */	public String getMemo() {		return memo;	}	/**	 * @return the result	 */	public String getResult() {		return result;	}}